import React from 'react';

import './spinner.scss';
// si on n'a rien à mettre dans notre div, on utilise la syntaxe autofermante
const Spinner = () => (
  <div className="spinner" />

);

export default Spinner;
